---
title: "What is ReasonBlocks?"
description: "Set one environment variable, capture your agent's workflow, and train a cheaper task-specific model with automatic fallback to your frontier model when coverage runs out."
---

ReasonBlocks captures how your production agent handles **its specific task**, then trains a
**cheaper model** personalized to that workflow. This is not a generic small model or a shared
checkpoint. Integration is one environment variable. ReasonBlocks hosts trace reconstruction,
dataset mining, training, and serving on **your** golden traces.

## The problem

Production agents run the biggest model on every step because nothing smaller can be
trusted with the workflow. Most of what a mature agent does is deeply repetitive: the same
tools, the same verify scripts, the same repair moves against the same gate failures. You
pay frontier prices (often hundreds or thousands of dollars per run) for behavior a
task-specific model can learn from a few dozen recorded sessions of **that** agent doing
**that** job.

## How it works

<Steps>
  <Step title="Route traffic through ReasonBlocks">
    Copy your gateway URL from [app.reasonblocks.com](https://app.reasonblocks.com) and set
    it before running your agent:

    ```bash
    export ANTHROPIC_BASE_URL="https://gateway.<your-tenant>.reasonblocks.com"
    ```

    With `ANTHROPIC_BASE_URL` pointed at your hosted ReasonBlocks gateway, your agent speaks
    its native protocol directly to ReasonBlocks. Your Anthropic API key passes through
    untouched and is never stored. Every exchange is captured to hosted storage. The gateway
    is byte-transparent and fail-open: if capture ever breaks, your traffic still flows.

    Add the two-line client shim in the same step so each request carries its run id — that
    is what makes a session provably complete later, and what most of your training data
    depends on. See [Integrating your agent](/client-integration).
  </Step>
  <Step title="Traces reconstruct themselves">
    ReasonBlocks reconstructs raw traffic into verified runs on hosted storage: ordered steps,
    tool calls paired to their results, and injected corrections identified structurally. Runs
    are labeled **golden** (proven complete, no violations) or **not**. Only golden runs from
    your workflow are eligible for training. You can review status in the dashboard; no local
    database required.
  </Step>
  <Step title="Policies are mined from golden traces">
    Tool vocabulary, helper scripts, and flat (state → action) pairs are extracted from
    **your** traffic, not configured by hand. When you have enough golden traces from this
    task (or on a schedule you set in the dashboard), ReasonBlocks starts mining automatically.
  </Step>
  <Step title="A cheaper task-specific model is trained">
    The hosted pipeline mines (state → action) pairs from complete runs and fine-tunes a
    smaller model for your tenant on **your** workflow. Training is opt-in and tenant-scoped.
  </Step>
  <Step title="Your trained model goes live">
    After training completes, the gateway serves your **trained model** through the same URL.
    Your frontier model stays connected as backup when coverage runs out or an edge case
    appears. Those episodes become training signal so your model improves over time. Same
    `ANTHROPIC_BASE_URL`; your agent code unchanged. Weights stay scoped to your tenant.
  </Step>
</Steps>

## What makes it trustworthy

- **Completeness is proven, not assumed.** A run with a hole in it is never mined. The
  reconstruction layer proves each run was captured whole before the pipeline may quantify
  over it.
- **Actions are validated against your workflow.** Sampled steps must use known tools,
  real helper scripts, and well-formed arguments from vocabulary mined from your own traces.
- **Whole-action selection only.** Rejection sampling keeps or drops complete actions.
  Nothing selects on partial steps, which avoids low-quality training pairs.
- **Spend ceilings everywhere.** Training jobs are cancelled the moment the provider's own
  cost estimate crosses your cap. GPU deployments tear down in `finally`.

## Get started

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Dashboard URL → env var → capture → distill → serve.
  </Card>
  <Card title="Integrating your agent" icon="plug" href="/client-integration">
    The gateway env var plus the two-line shim.
  </Card>
  <Card title="Endpoint compatibility" icon="network-wired" href="/endpoint-compatibility">
    Anthropic, OpenAI, Gemini, and other LLM API paths.
  </Card>
  <Card title="Deployment" icon="server" href="/deployment">
    Hosted gateway and pipeline (default); self-hosting appendix.
  </Card>
</CardGroup>
